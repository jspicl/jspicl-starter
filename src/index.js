/* eslint-disable no-underscore-dangle */
function _init () {
  // Perform initialization (configuration, game objects)
}

function _update60 () {
  // Perform updates (input, logic, physics)
}

function _draw () {
  cls();
  print("hello world!");
}

// <!-- DEBUG
// DON'T REMOVE THE CODE BELOW. THEY WILL AUTOMATICALLY BE STRIPPED OUT
_init();
_update60();
_draw();
